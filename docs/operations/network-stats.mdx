---
title: Network stats
description: Operator observability on the network — pool health, throughput, public snapshot.
icon: gauge
---

# Network stats

> Operator observability on the network — pool health, throughput, public snapshot.

The public snapshot that powers [malibu.tech/network](https://malibu.tech/network) is:

```
GET https://api.malibu.tech/v1/stats/overview
```

Same payload on `https://malibu.tech/v1/stats/overview`. Buyer API reference: [API → Network stats](/api/network-stats). `GET /v1/network-stats` is a compatibility alias for the same overview body.

## What's tracked (public)

| Signal | How it's counted |
| --- | --- |
| `nodes_online` | Providers with an active connection at snapshot time |
| `models_serving` | Distinct models advertised warm |
| `requests_total` / `tokens_*_total` | Lifetime billed volume on this surface |
| `nodes_hardware_attested` | Hardware-attested providers — **0** until that gate ships |
| `timeseries.rpm_30m` | Requests per minute, 60 s buckets |

Snapshot cadence is about 30 seconds (`stale_after` in the body).

## Warm models vs rate card vs catalog

These three lists are not the same:

| Surface | What it shows |
| --- | --- |
| `GET https://api.malibu.tech/v1/models` (Bearer) | Models with **at least one warm provider right now** |
| `GET https://api.malibu.tech/v1/rate-card` | Models the network **will bill** (eligible pricing rows) |
| Signed catalog feed | Models providers **may load** (hash, RAM tier, templates) |

A rate-card row can exist with zero warm providers. Buyer requests then fail until a Mac loads that model.

## Receipts and settlement

Receipt validity still drives [verified model settlement](/operations/verified-model-settlement). The public stats overview does **not** currently publish `valid_rate_last_24h`. Operators still have per-provider receipt views internally.

## Provider-level breakdown

Not exposed on the public snapshot. Operator dashboards can drill to:

* Uptime last 24 h / 7 d
* Requests served, faults, receipt validity
* Current model, RAM tier, autoupdate status
* Last-heartbeat delta
* Provisional vs pinned routing weight

## Changelog

Semantic changes to the public stats payload are noted in the [docs changelog](/changelog).

## Related

* [Reputation](/network/reputation) — how per-provider signals surface publicly
* [Metering & billing](/network/metering-and-billing) — how "billed request" is defined
* [Verified model settlement](/operations/verified-model-settlement) — how receipt validity affects settlement
